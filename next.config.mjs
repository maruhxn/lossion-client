/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com",
      "k.kakaocdn.net",
      "phinf.pstatic.net",
      "localhost",
    ],
  },
};

export default nextConfig;
